<Window x:Class="SportClub.Views.AddClubMemberWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Dodavanje Člana" Height="400" Width="400"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="TextBox" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
            <Setter Property="Margin" Value="0,5,0,15"/>
        </Style>
        <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignOutlinedButton}">
            <Setter Property="Margin" Value="10,0,0,0"/>
            <Setter Property="Width" Value="100"/>
        </Style>
        <Style TargetType="CheckBox" BasedOn="{StaticResource MaterialDesignCheckBox}">
            <Setter Property="Margin" Value="0,10,0,20"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Ime -->
        <TextBlock Grid.Row="0" Text="Ime:" Margin="0,0,0,5" FontWeight="Bold"/>
        <TextBox x:Name="ImeTextBox" Grid.Row="1"/>

        <!-- Prezime -->
        <TextBlock Grid.Row="2" Text="Prezime:" Margin="0,0,0,5" FontWeight="Bold"/>
        <TextBox x:Name="PrezimeTextBox" Grid.Row="3"/>

        <!-- Datum rođenja -->
        <TextBlock Grid.Row="4" Text="Datum rođenja:" Margin="0,0,0,5" FontWeight="Bold"/>
        <DatePicker x:Name="DatumRodjenjaDatePicker" Grid.Row="5" 
                   Style="{StaticResource MaterialDesignOutlinedDatePicker}"
                   Margin="0,5,0,10"/>

        <!-- Aktivan -->
        <CheckBox x:Name="AktivanCheckBox" Grid.Row="6" Content="Aktivan" IsChecked="True"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Sačuvaj" Click="SaveButton_Click"/>
            <Button Content="Otkaži" Click="CancelButton_Click" Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>