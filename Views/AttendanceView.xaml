<UserControl x:Class="SportClub.Views.AttendanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Upravljanje prisustvom" 
                   FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Filter controls -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="SearchDateTextBox" Width="120" Margin="0,0,10,0"
                     materialDesign:HintAssist.Hint="Datum (dd.MM.yyyy)"
                     Text="{Binding SearchDate, UpdateSourceTrigger=PropertyChanged}"/>

            <ComboBox Width="120" Margin="0,0,10,0"
                      ItemsSource="{Binding TrainingTypes}"
                      SelectedItem="{Binding SelectedTrainingType}"
                      materialDesign:HintAssist.Hint="Tip treninga"/>

            <Button Content="Novi trening" Click="AddTraining_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>

        <!-- Trainings list -->
        <DataGrid Grid.Row="2" AutoGenerateColumns="False" 
                  ItemsSource="{Binding Trainings}" 
                  SelectedItem="{Binding SelectedTraining}"
                  IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Naziv" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Tip" Binding="{Binding Type}" Width="100"/>
                <DataGridTextColumn Header="Datum i vrijeme" Binding="{Binding DateTime}" Width="150"/>
                <DataGridTextColumn Header="Prisutni" Binding="{Binding Attendances.Count}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Add member section -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,10">
            <ComboBox x:Name="MembersComboBox" Width="200" Margin="0,0,10,0"
                      DisplayMemberPath="FullName"
                      materialDesign:HintAssist.Hint="Odaberite člana"/>
            <Button Content="Dodaj člana na trening" Click="AddMember_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>

        <!-- Attending members list -->
        <DataGrid Grid.Row="4" AutoGenerateColumns="False" 
                  ItemsSource="{Binding AttendingMembers}"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ime" Binding="{Binding FirstName}" Width="*"/>
                <DataGridTextColumn Header="Prezime" Binding="{Binding LastName}" Width="*"/>
                <DataGridTextColumn Header="Datum rođenja" Binding="{Binding BirthDate, StringFormat=d}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>