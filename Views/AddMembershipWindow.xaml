<Window x:Class="SportClub.Views.AddMembershipWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Dodavanje Članarine" Height="300" Width="384" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Naziv:" Grid.Row="0" Margin="0,10,0,5"/>
        <TextBox x:Name="NazivTextBox" Grid.Row="1" Margin="0,0,0,10"/>

        <TextBlock Text="Cijena (€):" Grid.Row="2" Margin="0,0,0,5"/>
        <TextBox x:Name="CijenaTextBox" Grid.Row="3" Margin="0,0,0,10" PreviewTextInput="CijenaTextBox_PreviewTextInput"/>

        <TextBlock Text="Trajanje (dani):" Grid.Row="4" Margin="0,0,0,5"/>
        <TextBox x:Name="TrajanjeTextBox" Grid.Row="5" Margin="0,0,0,20" PreviewTextInput="TrajanjeTextBox_PreviewTextInput"/>

        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Sačuvaj" Click="SaveButton_Click" Width="80" Margin="0,0,10,0"/>
            <Button Content="Otkaži" Click="CancelButton_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>