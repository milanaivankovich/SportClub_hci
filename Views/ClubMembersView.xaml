<UserControl x:Class="SportClub.Views.ClubMembersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:SportClub.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Buttons Row -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Pregled Članova" Command="{Binding PreviewCommand}" Width="180" Height="30" Margin="5"/>
            <Button Content="Dodavanje Člana" Command="{Binding AddCommand}" Width="180" Height="30" Margin="5"/>
            <Button Content="Izmjena Člana" Command="{Binding EditCommand}" Width="180" Height="30" Margin="5"/>
        </StackPanel>
        <!-- Data Grid -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding ClubMembers}" SelectedItem="{Binding SelectedClubMember}" AutoGenerateColumns="False" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ime" Binding="{Binding FirstName}" Width="*"/>
                <DataGridTextColumn Header="Prezime" Binding="{Binding LastName}" Width="*"/>
                <DataGridTextColumn Header="Datum rođenja" Binding="{Binding BirthDate, StringFormat='dd/MM/yyyy'}" Width="*"/>
                <DataGridCheckBoxColumn Header="Aktivan" Binding="{Binding Active}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>