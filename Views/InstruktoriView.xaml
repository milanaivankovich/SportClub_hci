<UserControl x:Class="SportClub.Views.InstruktoriView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:SportClub.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
		<!-- Buttons Row -->
		<StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
			<Button Content="Pregled Instruktora" Command="{Binding PreviewCommand}" Width="180" Height="30" Margin="5"/>
			<Button Content="Dodavanje Instrukora" Command="{Binding AddCommand}" Width="180" Height="30" Margin="5"/>
			<Button Content="Izmjena Instrukora" Command="{Binding EditCommand}" Width="180" Height="30" Margin="5"/>
		</StackPanel>
		<!-- Data Grid -->
		<DataGrid Grid.Row="1" ItemsSource="{Binding Instructors}" SelectedItem="{Binding SelectedInstructor}" AutoGenerateColumns="False" Margin="10">
			<DataGrid.Columns>
				<DataGridTextColumn Header="Korisničko ime" Binding="{Binding Username}" Width="*"/>
				<DataGridTextColumn Header="Ime" Binding="{Binding FirstName}" Width="*"/>
				<DataGridTextColumn Header="Prezime" Binding="{Binding LastName}" Width="*"/>
				<DataGridTextColumn Header="Lozinka" Binding="{Binding Password}" Width="*"/>
			</DataGrid.Columns>
		</DataGrid>
	</Grid>
</UserControl>