<UserControl x:Class="SportClub.Views.CompetitionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:SportClub.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Buttons Row -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Pregled Takmičenja" Command="{Binding PreviewCommand}" Width="180" Height="30" Margin="5"/>
            <Button Content="Dodavanje Takmičenja" Command="{Binding AddCommand}" Width="180" Height="30" Margin="5"/>
            <Button Content="Izmjena Takmičenja" Command="{Binding EditCommand}" Width="180" Height="30" Margin="5"/>
        </StackPanel>
        <!-- Data Grid -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Competitions}" SelectedItem="{Binding SelectedCompetition}" AutoGenerateColumns="False" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Naziv" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Mjesto" Binding="{Binding Location}" Width="*"/>
                <DataGridTextColumn Header="Datum i vrijeme" Binding="{Binding Date, StringFormat='dd/MM/yyyy HH:mm'}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>